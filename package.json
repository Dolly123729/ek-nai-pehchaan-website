{
  "name": "ek-nai-learning-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Static learning website with a secure OpenAI-powered chatbot backend.",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "check": "node --check server.js && node --check chatbot.js"
  }
}
